function calculateaverage() {
  const ragaca = document.querySelector("form");

  const ragacaa = (math + english) / 2;
  document.getElementById("result").innerHTML = "student" + "name" + "average";
}
